import { createMuiTheme } from "@material-ui/core";
//// material-ui  change default color
const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#0061ff",
    },
  },
});

export default theme;
